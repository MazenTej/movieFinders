import React from "react";

const DropdownMenu = () => {
  return (
    <div className="dropdown-menu">
        <a>Profile</a>
        <a>Favorites</a>
        <a>Sign out</a>
    </div>
  );
};

export default DropdownMenu;